rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /shared/{filename} {
      // Only authenticated users can upload; download requires auth too
      allow read, write: if request.auth != null;
    }
  }
}
